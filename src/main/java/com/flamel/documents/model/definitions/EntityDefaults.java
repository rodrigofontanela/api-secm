package com.flamel.documents.model.definitions;

public final class EntityDefaults {

    public static class Columns {
        public static class Validations {
            public static final String DEFAULT_SIZE_VALIDATION_MESSAGE = "O tamanho do dado informado (${validatedValue.length()} caracteres) excede o máximo permitido ({max} caracteres)";
        }
    }
}
